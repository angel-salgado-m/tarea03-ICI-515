
#include <global.hh>
#include <vector>

// Definir las variables globales
std::vector<Caja> cajas;
int numCajas;

int total_a = 0;  // Inicialización del primer contador global.
int total_b = 0;  // Inicialización del segundo contador global.
int e = 0;        // Inicialización del contador de eventos.
